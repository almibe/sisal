<!doctype html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Demo</title>
        <script type="module" src="/assets/main.js"></script>
    </head>
    <body>
        Hello {{greeting.value}}!

        {% renderTemplate "wander" %}
        (is-consistent (definitions) (assertions [betty : Cat] [betty ~ Cat]))
        {% endrenderTemplate %}

        {% renderTemplate "wander" %}
        { display draw network (assertions [a b c]) }
        {% endrenderTemplate %}
    </body>
</html>
